const initialState = {name:'English' , price:2000};

const CourseReducer = (state = initialState , action) => {
  switch(action.type) {
    default:
        return state;
}
}

export default CourseReducer;